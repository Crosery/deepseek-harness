/** Host loader entry for the terminal attachment plugin (no host-side behavior). */

/** Host plugin body — the terminal client half owns all behavior. */
export function apply(_ctx: unknown): void {}
